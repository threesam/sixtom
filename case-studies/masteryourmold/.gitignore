# Heavy capture artifacts — ~85MB, regenerable from the scripts in this directory.
# The committed set (00-baseline.md, before.json, scorecard.png, raw/*.json) carries
# every number cited in the writeup; these are the underlying blobs behind them.
# Reproducibility is the receipt, not stored megabytes — see §0 of 00-baseline.md.
shots/
lighthouse/
raw/*.html

# Regenerated by build-scorecard.mjs; embeds the screenshots as base64, so it's a
# ~1MB duplicate of scorecard.png. The PNG is the artifact of record.
scorecard.html
after.html
