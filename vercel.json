{
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/static-build",
			"config": {
				"distDir": "build",
				"installCommand": "pnpm install",
				"buildCommand": "pnpm run vercel-build"
			}
		}
	]
}
